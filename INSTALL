Scripts to generate Debian packages are provided as well, run

./build-package.sh

Or you can compile this program by hand if you want to:

./configure && make

In both cases the system should detect automatically if running in
Maemo 3, Maemo 4 or a standard desktop system.
